# Django Development Environment Settings

# Core settings
DEBUG=True
SECRET_KEY=django-insecure-h1w8&!xv2u5z+z7r-*!%p&br_4%dv4$t(b(@0$pwshr=ahfvnf
ALLOWED_HOSTS=localhost,127.0.0.1

# Database settings - SQLite is used in development by default
# If you want to use PostgreSQL in development, uncomment and set these:
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=ram_naam_jaap_dev
# DB_USER=postgres
# DB_PASSWORD=postgres
# DB_HOST=localhost
# DB_PORT=5432

# Cache settings
# Default is LocMemCache, for Redis, uncomment:
# CACHE_URL=redis://localhost:6379/1

# Media and static files
# Leave blank for default locations (within project)
# MEDIA_ROOT=
# STATIC_ROOT=

# Email settings (console backend used for development)
# For actual email sending in development, uncomment:
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password
# DEFAULT_FROM_EMAIL=Ram Naam Jaap <your-email@gmail.com>

# Logging
LOG_LEVEL=DEBUG
# LOG_FILE=

# Django settings
DATABASE_URL=postgres://ram_user:your_password@localhost:5432/ram_naam_jaap

# Redis settings
REDIS_URL=redis://localhost:6379/0
CELERY_BROKER_URL=redis://localhost:6379/1

# Email settings
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
# For production:
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your_email@example.com
# EMAIL_HOST_PASSWORD=your_email_password

# Admin URL paths
ADMIN_URL=durga/
DASHBOARD_URL=admin/

# Static and media files
STATIC_URL=/static/
MEDIA_URL=/media/

# Tailwind CSS
TAILWIND_DEV_MODE=True

# Security (for production)
# CSRF_COOKIE_SECURE=True
# SESSION_COOKIE_SECURE=True
# SECURE_SSL_REDIRECT=True
# SECURE_HSTS_SECONDS=31536000 